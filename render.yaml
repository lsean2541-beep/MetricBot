services:
  - type: worker
    name: metrict-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false # You will add the actual token in the Render UI
